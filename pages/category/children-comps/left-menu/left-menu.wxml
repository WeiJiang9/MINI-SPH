<scroll-view scroll-y class="left-menu">
  <block wx:for="{{leftMenuList}}" wx:key="item">
    <view class="menu-item {{index === currentIndex ? 'active' : ''}}" bind:tap="handleItemTap"  data-index="{{index}}">{{item}}</view>
  </block>
</scroll-view>